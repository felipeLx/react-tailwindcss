import React from 'react';

const Navbar: React.FunctionComponent = (props: any) => (
    <section>
        <nav>
            <ul>
                <li>Home</li>
            </ul>
        </nav>
    </section>
);

export default Navbar;